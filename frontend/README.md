# RideAlongUG Frontend (Angular)

Angular 19 standalone rebuild of the RideAlongUG rental platform frontend.

## Setup

npm install
npm start

The dev server runs on port 5173. Configure the API base URL in `src/app/core/api.service.ts`
(defaults to `http://localhost:8080/api/v1/`).
